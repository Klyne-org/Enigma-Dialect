## Auto-generated by CMake — do not edit the .py version, only the .py.in
##
## WHAT THIS FILE IS
##   A template that CMake's `configure_lit_site_cfg()` copies into the
##   build directory as `lit.site.cfg.py`, substituting `@...@` tokens
##   with real paths. The substituted file is what lit reads at startup
##   before handing off to lit.cfg.py in the source tree.
##
##   Think of it as: "injectable config values that depend on where the
##   build happened." lit.cfg.py is "test logic that is the same for any
##   build."

import sys

config.llvm_tools_dir    = lit_config.substitute("@LLVM_TOOLS_DIR@")
config.mlir_obj_root     = lit_config.substitute("@MLIR_BINARY_DIR@")
config.enigma_src_root   = lit_config.substitute("@CMAKE_SOURCE_DIR@")
config.enigma_obj_root   = lit_config.substitute("@CMAKE_BINARY_DIR@")
config.enigma_tools_dir  = lit_config.substitute("@CMAKE_BINARY_DIR@/bin")

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Delegate the rest of the configuration to lit.cfg.py.
lit_config.load_config(config, "@CMAKE_SOURCE_DIR@/test/lit.cfg.py")
